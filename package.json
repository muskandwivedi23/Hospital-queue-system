{
  "name": "medflow-system",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace management for MedFlow Queue System",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run start --prefix backend",
    "backend:watch": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "start": "npx concurrently \"npm run backend:watch\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
